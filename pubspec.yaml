name: guidi_client
description: Flutter toolkit for the Guidi guide tracking API. Includes API client, guide models, service layer, and coach mark UI.
version: 0.2.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  tutorial_coach_mark: ^1.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
